<Page
    x:Class="Dictionary.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dictionary.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <local:MainViewModel/>
    </Page.DataContext>

    <Grid Background="#FFDEDEDE">
        <TextBlock Text="DICTIONARY"
                   FontSize="90"
                   FontFamily="Century Gothic"
                   FontWeight="ExtraLight"
                   TextAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,30,0,0"/>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <!-- <ComboBox ItemsSource="{x:Bind sourceLanguages}"/> -->
            <TextBox Text="{Binding Word, Mode=TwoWay}"
                 FontSize="25"
                 VerticalAlignment="Top"
                 BorderBrush="AliceBlue"
                 Width="600"
                 Margin="0,170,0,0"
                 PlaceholderText="search for a word..."/>
        </StackPanel>
        <TextBlock Text="{Binding Status, Mode=TwoWay}"
                   TextAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,250,0,0"/>
        <ListView ItemsSource="{Binding Meanings, Mode=OneWay}"
                  Padding="40,10"
                  Margin="0,260,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               FontSize="15"
                               FontFamily="Century Gothic"
                               TextWrapping="WrapWholeWords"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

